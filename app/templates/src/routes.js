module.exports = function( router ) {
    // This is where your routes go.
    // They should like something like this:
    //
    //   router.resources( 'bananas' );
    //   router.get( '/', 'home#index' );
    //
    // The above resources method asumes that you have a controller in
    // src/controllers named bananas.js that defines a JavaScript object with
    // at least some of the stanard resource actions.
    //
    // The above get method declares an explicit mapping between '/' and the
    // home controller's index method
    //
    // You can read more about controllers/actions:
    // http://github.com/tybenz/paper-router
};
